import { Stack } from '@mui/material';
import React from 'react';

const RoundsSummary = () => {
    return (
        <Stack direction ='row'>
            
        </Stack>
    );
};

export default RoundsSummary;